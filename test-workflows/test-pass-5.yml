name: "Test PASS 5 - Should NOT run with fail-fast"
version: "1.0"
variables:
  base_url: "http://httpbin.org"
steps:
  - name: "Step 1 - Should NOT run if fail-fast works"
    request:
      method: "POST"
      url: "{{base_url}}/post"
      body:
        test: "data"
    validate:
      - status: 200

