name: "Test PASS 1"
version: "1.0"
variables:
  base_url: "http://httpbin.org"
steps:
  - name: "Step 1 - Should PASS"
    request:
      method: "GET"
      url: "{{base_url}}/status/200"
    validate:
      - status: 200

